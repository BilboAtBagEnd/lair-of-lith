<% @title = "Viewing #{@character.name} v#{@version.version} by #{@owner.name}" %>
<h2><%= @character.name %></h2>
<h3>by <%= link_to @owner.name, user_path(@owner) %></h3>
<h3>Version <%= @version.version %></h3>

<p><%= link_to 'All versions', controller: 'characters', action: 'view', id: @character.id  %></p>

<%= render 'characters/generator' %>
